<%= select(:datasheet_category_id, nil, { }, {"v-model":"selectedCategory", "v-on:change": "fetchDatasheets", class:"form-control", id:"categories_menu"}) do %>
  <%= options_for_select(
    @datasheet_categories.collect { |d| [d.name, d.id]}, @datasheet_categories[0].id
  )
  %>
<% end %>
